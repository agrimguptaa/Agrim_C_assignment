#include <stdio.h>
#include"agrim.h"
int main()
{
    int num;
    scanf("%d",&num);
    neon(num);//to check neon
    palindrome(num);//to check palindrome
    ugly_number(num);//to check ugly number
}